const baseUrl = process.env.REACT_APP_API

const urls = {
    character: '/character',
    characters: '/characters',
    episodes: '/episodes',
    episode: '/episode'
 
}


export {
    baseUrl,
    urls
}